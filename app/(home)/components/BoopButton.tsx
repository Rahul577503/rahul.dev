"use client";

const AudioPlayer = () => {
  // Placeholder component - sound functionality disabled due to React 19 compatibility
  // Can be re-enabled when use-sound supports React 19

  return null;
};

export default AudioPlayer;
